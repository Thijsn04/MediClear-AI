# MediClear AI — Core runtime dependencies
#
# This file installs ONLY the core server. AI provider SDKs and optional
# features are extras so you install just what your deployment needs.
#
#   pip install -r requirements.txt                 # core only
#   pip install '.[openai]'                          # + OpenAI-compatible (Ollama, Azure, Groq…)
#   pip install '.[gemini]'                          # + Google Gemini
#   pip install '.[anthropic]'                       # + Anthropic Claude
#   pip install '.[redis]'                           # + durable sessions/cache/rate-limit
#   pip install '.[ocr]'                             # + OCR for scanned PDFs
#   pip install '.[tts-cloud]' | '.[tts-local]'      # + text-to-speech
#   pip install '.[metrics]'                         # + Prometheus /metrics
#   pip install '.[all,dev]'                         # everything + test/lint tooling
#
# See pyproject.toml [project.optional-dependencies] for the authoritative list.

# ── Web framework ──────────────────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.27

# ── Configuration ─────────────────────────────────────────────────────────
pydantic-settings>=2.4.0
python-dotenv>=1.0.0

# ── Document processing ────────────────────────────────────────────────────
pypdf>=4.0.0
Pillow>=10.0.0

# ── Observability ─────────────────────────────────────────────────────────
structlog>=24.0.0

# ── HTTP client (async, used internally) ──────────────────────────────────
httpx>=0.27.0
